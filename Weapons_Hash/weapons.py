import sys
import os

# Add the directory to sys.path to allow importing the modules
module_dir = r"E:\Windows\Documents\D2 API\Weapons_Hash"
sys.path.append(module_dir)

# Import the modules in the specified order
import weapon_hashes_unquie
import weapon_hashes
import weapon_hashes_final